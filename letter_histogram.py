def letter_histogram = ('Banana')
histogram = {}
for l in word:
    if l in histogram:
        histogram[1] = histogram[1] + 1
        else:
            histogram[1] = 1
        return histogram
    print(letter_histogram('abracadabbaraucda'))